<h2>Users</h2>
	<table class="table">
		<thead>
			<% users.each do |user| %>
			<tr>
				<td>
					 <%= user.username %>
				</td>
				<td>
					<%= link_to 'Show', user_path(user.id), class: "user_#{user.id}" %>
				</td>
			</tr>
			<% end %>
		</thead>
	</table>